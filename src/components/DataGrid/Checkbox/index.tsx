'use client'

import { Checkbox, CheckboxProps } from '@mui/material'
import React from 'react'

function DataGridCheckbox(props: CheckboxProps) {
  return <Checkbox color="marine" {...props} />
}

export default DataGridCheckbox
